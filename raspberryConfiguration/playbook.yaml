- name: My first play
  hosts: faceit.local
  remote_user: pi
  become: true
  gather_facts: False #To disable first task
  tasks:
    - name: Install PIP3
      apt:
        name: python3-pip
        state: present
        update_cache: yes
    - name: Install GIT
      apt:
        name: git
        state: present
        update_cache: yes
    - name: Install Android Debug Bridge (ADB)
      apt:
        name: android-sdk-platform-tools
        state: present
        update_cache: yes
    - name: Clone single branch from repo
      ansible.builtin.git:
        repo: https://github.com/CaishenNefri/csgo-faceit-monitor-matches.git
        dest: /home/smsNotify
        single_branch: yes
        version: feature/smsNotification
    - name: Execute permission for smsApp - python requriments
      file: dest=/home/smNotify/smsNotifyApplication/requirements.txt mode=a+x
    - name: Execute permission for smsApp - python main.py
      file: dest=/home/smNotify/smsNotifyApplication/main.py mode=a+x
    - name: Install smsNotify app requriments
      ansible.builtin.pip:
        requirements: /home/smNotify/smsNotifyApplication/requirements.txt